name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master  # Adjust this to match your repository's main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: 2.7  # Adjust to match your Ruby version

      - name: Install dependencies
        run: |
          gem install bundler
          bundle install
        working-directory: ./  # Adjust to your site's directory

      - name: Build and deploy
        run: |
          bundle exec jekyll build
          echo 'Satwik-Kar.github.io' > ./docs/CNAME  # Optional for custom domains
        working-directory: ./  # Adjust to your site's directory

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.ACCESS_TOKEN }}
          publish_dir: ./  # Adjust to your site's build directory
